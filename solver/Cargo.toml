[package]
name = "poker_solver"
version = "0.1.0"
edition = "2021"

[dependencies]
postflop-solver = { path = "../postflop-solver", features = ["rayon"] }
rusqlite = "0.31"
# Note: Enabled default features which includes "bincode" for save/load
